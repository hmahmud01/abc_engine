This project is for the code challange for iDare interview for React developer

# first run `npm install` to install packages
# then run `npm start` to run the project
# redirect to `localhost:3000`

done!
